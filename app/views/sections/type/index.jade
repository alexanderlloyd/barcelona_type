extends ../../layouts/_type

block content          
    main           
      ul.gallery__list
        li.gallery__item(ng-repeat='p in pics | limitTo: 12')
          img(ng-src='{{p.images.standard_resolution.url}}')   
